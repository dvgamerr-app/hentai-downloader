export const onLoaded = state => state.onEvent.loaded

export const alert = state => state.onEvent.alert

export const offline = state => state.onEvent.offline

export const onWait = state => state.onEvent.wait
